{
  "name": "zhongzhuan-android",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:android": "cap sync android",
    "open:android": "cap open android",
    "build:debug": "npm run sync:android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
